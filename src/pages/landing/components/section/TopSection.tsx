import BottomContent from '../top-section/BottomContent';
import TopGraphic from '../top-section/TopGraphic';

export default function TopSection() {
  return (
    <section>
      <TopGraphic />
      <BottomContent />
    </section>
  );
}
