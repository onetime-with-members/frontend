import BottomContent from './BottomContent';
import TopGraphic from './TopGraphic';

export default function TopSection() {
  return (
    <section>
      <TopGraphic />
      <BottomContent />
    </section>
  );
}
