import BottomContent from './BottomContent/BottomContent';
import TopGraphic from './TopGraphic/TopGraphic';

export default function TopSection() {
  return (
    <section>
      <TopGraphic />
      <BottomContent />
    </section>
  );
}
